import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100%;

  input {
    width: 100%;
    height: 2rem;
    padding: 0 1rem;
    border: 1px solid var(--gray-300);
  }
`;
