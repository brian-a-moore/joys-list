import styled from 'styled-components';

function List() {
    return (
        <Wrapper>
            <span>List</span>
        </Wrapper>
    );
}

export default List;

const Wrapper = styled.section``;
